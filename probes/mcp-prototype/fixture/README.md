# PocketBrain (M0 fixture)

A deliberately tiny stand-in for the real vault, used only so the probe server has
something real to read. The full vault template arrives in M2.

| Folder | Holds |
|---|---|
| `00-Inbox` | Human-created notes |
| `10-Drafts` | AI output, before review |
| `20-Sources` | Verified reference material (read-only to AI) |
